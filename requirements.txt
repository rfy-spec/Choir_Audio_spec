# =========================================================
# Choir Audio Evaluation - Traditional Acoustics Stage
# 童声合唱自动评估（传统声学规则阶段）
#
# 说明：
# - 本阶段不涉及深度学习（不安装 torch / tensorflow 等）
# - 采用“主版本区间锁定”策略，保证稳定性 + 未来可升级性
# - 等评分规则定型或进入比赛/论文阶段，再进一步精确锁死版本
# =========================================================

# -------- 数值计算与科学计算基础 --------
numpy>=1.23,<2.0
scipy>=1.9,<2.0

# -------- 音频读取与传统声学特征 --------
librosa>=0.10,<0.11
soundfile>=0.12,<0.14

# -------- 可解释结果可视化 --------
matplotlib>=3.6,<4.0

# -------- 工程工具（进度条 / 测试）--------
tqdm>=4.64,<5.0
pytest>=7.0,<9.0

# -------- 可选：Praat 接口（高级音质/发声特征）--------
# 如果后续需要更精确的 jitter / shimmer / HNR / CPP，可手动安装
praat-parselmouth>=0.4,<0.5
